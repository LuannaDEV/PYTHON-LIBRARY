[tool.poetry]
name = "python-library-atividade"
version = "0.1.0"
description = ""
authors = ["itsthoda <mo4na12000@gmail.com>"]
package-mode = false        

[tool.poetry.dependencies]
python = ">=3.12"
fastapi = {version = ">=0.134.0,<0.135.0", extras = ["standard"]}
sqlalchemy = ">=2.0.0"
uvicorn = ">=0.41.0"
redis = "^8.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"